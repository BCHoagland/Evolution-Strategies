virtual batch normalization => not brittle anymore
sample ε and -ε instead of just ε (mirror sampling)
transform fitness scores (F_i) into just ranks to reduce the effect of outlier performers
weight decay to network params
fix σ as a hyperparameter
virtual batch normalization